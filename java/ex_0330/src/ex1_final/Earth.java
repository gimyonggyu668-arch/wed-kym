package ex1_final;

public class Earth {

    // 지구의 반지름 (단위: km)
    // 상수는 관례적으로 대문자와 언더바(_)를 사용합니다.
    static final double EARTH_RADIUS = 6400;

    // 지구의 표면적 (공식: 4 * pi * r^2)
    static final double EARTH_SURFACE_AREA = 4 * Math.PI * Math.pow(EARTH_RADIUS, 2);

    // 메인 메소드: 결과를 확인하기 위한 테스트용
   
    
    }
